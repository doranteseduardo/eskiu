// Generic free-function monomorphization: id<int>/add<int> -> @id_int/@add_int.
T id<T>(T x) { return x; }
int add<T>(T a, T b) { return a + b; }

int main() {
    return id<int>(40) + add<int>(1, 1);
}
