// EXPECT-ERROR: has no enclosing loop labeled
// `break`/`continue` naming a label with no enclosing loop of that name is rejected.
int main() {
    outer: for (int i = 0; i < 3; i = i + 1) {
        for (int j = 0; j < 3; j = j + 1) {
            break nope;
        }
    }
    return 0;
}
