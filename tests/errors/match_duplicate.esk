// A match with two arms for the same variant is rejected.
// EXPECT-ERROR: duplicate match arm
enum Tag { A(int), B }
int f(Tag t) {
    match t {
        A(x) -> { return x; }
        A(y) -> { return y; }
        B    -> { return 0; }
    }
    return 0;
}
int main() { return f(B); }
