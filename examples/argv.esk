extern int printf(string fmt, ...);

int main(int argc, string* argv) {
    printf("argc = %d\n", argc);

    int i = 0;
    while (i < argc) {
        printf("argv[%d] = %s\n", i, argv[i]);
        i = i + 1;
    }

    return 0;
}
