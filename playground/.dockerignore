# Keep the build context small (it's the repo root). The compiler is rebuilt
# from source inside the image, so local build artifacts and docs aren't needed.
build/
.git/
site/
docs/
tests/
editor/
*.o
*.out
**/__pycache__/
