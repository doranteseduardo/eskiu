extern i32 printf(i8* fmt);

i32 add(i32 a, i32 b) {
    return a + b;
}

i32 main() {
    i32 x = 5;
    i32 y = 10;
    i32 result = add(x, y);
    printf("x=%d, y=%d, sum=%d\n", x, y, result);
    return 0;
}
